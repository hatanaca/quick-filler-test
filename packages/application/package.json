{
  "name": "@quickfiller/application",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Application layer — use cases, DTOs, event bus",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@quickfiller/domain": "*"
  }
}
