{
  "name": "@quickfiller/domain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain layer — entities, value objects, domain services, ports",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
